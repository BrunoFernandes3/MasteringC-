@using Fiap.Web.AspNet2.Views.ViewModel
@model IEnumerable<RepresentanteViewModel>

<div class="container">
    <div class="row">
        <div class="col-md-12">

            <h1>Representantes</h1>
            @{
                var mensagem = (String)@TempData["mensagemSucesso"];
                if (( ! String.IsNullOrEmpty(mensagem)))
                    {
                        <div class="alert alert-warning" role="alert">
                        <h4>@mensagem</h4>
                        </div>
                    }
                
            }
            
            @if(@Model.Count() > 0){
            <table class="table table-striped" border="1" cellspacing="0" cellpadding="0">
                <thead>
                    <tr>
                        <th data-field="id">Id</th>
                        <th data-field="name">Nome</th>
                        <th class="actions" width="220">Ações</th>
                    </tr>
                </thead>
                <tbody>
                
                    @foreach (var item in @Model)
                     {
                            <tr>
                                <td>@item.RepresentanteId</td>
                                <td>@item.NomeRepresentante</td>
                                <td>
                                    <a class="btn btn-dark btn-sm" asp-controller="Representante" asp-action="Detalhe" asp-route-id="@item.RepresentanteId" >Detalhes</a>
                                    <a class="btn btn-warning btn-sm" asp-controller="Representante" asp-action="Editar" asp-route-id="@item.RepresentanteId">Editar</a>
                                    <a class="btn btn-danger btn-sm" asp-controller="Representante" asp-action="Excluir" asp-route-id="@item.RepresentanteId">Excluir</a>
                                </td>
                            </tr>
                     }
                </tbody>
            </table>
            }
            else{
                <div class="alert alert-warning" role="alert">
                     <h2>Sem clientes cadastrados.</h2>
                </div>
            }
            <p class="toolbar">
                <a class="create btn btn-primary" asp-controller="Representante" asp-action="Cadastrar">Novo Representante</a>
            </p>
        </div>
    </div>
</div>

